node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# 파생물 — md/json에서 빌드된다
content/.build/

# 로컬 DB
data/
*.sqlite
*.sqlite-journal

# 파생물 — npm run preview로 다시 뽑는다
preview/

# 비밀이 든다. 절대 커밋되면 안 된다 — ADMIN_TOKEN이 여기 있다.
.env*.local
